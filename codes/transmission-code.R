newdata <- data.frame(hp = hp, wt = wt)

         answer <- predict(model, newdata, type = 'response')newdata <- data.frame(hp = hp, wt = wt)

         answer <- predict(model, newdata, type = 'response')